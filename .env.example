# 能力集市运行期配置（复制到 <runtime>/backend/.env；启动脚本会 source 后 export）
# 数据库：**必须放在代码与 runtime 之外**（部署 rsync --delete 不会碰它；与 Brain 的 BRAIN_DATA_DIR 同款约定）
MARKETPLACE_DB=/Users/gaolei/database/home-agent-capability-marketplace/marketplace.sqlite3
# 监听口：本机/局域网访问 UI 与 API（平台登记用同一个口）
MARKETPLACE_PORT=4250
MARKETPLACE_HOST=0.0.0.0
